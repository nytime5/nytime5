{
  "private": true,
  "devDependencies": {
    "hugo-bin": "^0.138.0"
  },
  "scripts": {
    "check": "./scripts/check.sh",
    "deploy": "./scripts/check.sh && npx hugo && npx wrangler deploy",
    "preview": "npx hugo && npx wrangler dev"
  },
  "hugo-bin": {
    "version": "0.150.0"
  }
}
