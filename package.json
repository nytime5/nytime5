{
  "private": true,
  "devDependencies": {
    "hugo-bin": "^0.138.0",
    "wrangler": "^4.80.0"
  },
  "scripts": {
    "deploy": "npx hugo && wrangler deploy",
    "preview": "npx hugo && wrangler dev"
  }
}
